# The public build ships as a static demo with bundled sample data — no backend.
# To point a production build at a real backend instead, set VITE_DEMO=false and
# VITE_API_URL=https://your-api in your host's env vars.
VITE_DEMO=true
