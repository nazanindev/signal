[build]
builder = "nixpacks"

[deploy]
startCommand = "python -W ignore::FutureWarning -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
